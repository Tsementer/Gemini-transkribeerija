{
  "name": "gemini-transkribeerija-proxy",
  "version": "2.0.0",
  "description": "Function to generate signed URLs for Google Gemini file uploads",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/storage": "^7.11.0"
  }
}
